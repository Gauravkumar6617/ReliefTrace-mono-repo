# copy to .env and adjust if the API isn't running on the default port
VITE_API_BASE_URL=http://localhost:8000

# cluster used to build "Verify on-chain" Solana Explorer links
VITE_SOLANA_CLUSTER=devnet
